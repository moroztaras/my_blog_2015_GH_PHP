{% extends 'MorozBlogBundle::baseBlogTemplate.html.twig' %}
{% block title %}{{ post.title }} - Блог{% endblock %}
{% block body %}
        <h2>{{ post.title }}</h2>
        <p>{% trans %}Автор:{% endtrans %} {{ post.author }} | {% trans %}Створено:{% endtrans %} {{post.postedDate|date("d-m-Y")}} | {% trans %}Переглядів:{% endtrans %} {{ post.counter }} | <a href="{{ path('blog_post_edit',{postId:post.id}) }}">Редагувати</a></p>
        <p>{{ post.content }}</p>
{% endblock %}
